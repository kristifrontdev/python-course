Q1
І виникло  питання щодо модулів CommonJS. Чи звертають на таке увагу на практиці.

Наскільки знаю імпорти рекомендують розміщувати на початку файлу, а експорти в кінці. 
Однак як я зрозумів, require() в CommonJS працює синхронно, на відміну від ES6 модулів.

- Чи не може така синхронна загрузка численних (чи  "важких") модулів на старті блокувати потік і вплинути на продуктивність?
- Чи використовують може якісь підходи у CommonJS  для асинхронного завантаження модулів? Або ж це не настільки критично?

Q2
// Створіть файл string_check.js у цій теці. 
// У файлі реалізуйте анонімний модуль, який приймає рядок і повертає true, якщо він містить лише літери у нижньому регістрі.
// Якщо рядок містить хоча б один символ у верхньому регістрі — повертає false.
// Якщо рядок порожній — повертає true.